# === Kiro IDE specs (excluded from git, not from IDE access) ===
# Note: .kiro/ files are excluded via .git/info/exclude instead

# === Rust ===
target/
# Note: Cargo.lock is intentionally tracked (application, not library)

# === Go ===
picoclaw/picoclaw

# === Node.js ===
node_modules/
dist/
*.tsbuildinfo

# === Environment & Secrets ===
.env
.env.*
*.pem
*.key
terraform.tfvars
*.tfstate
*.tfstate.backup
.terraform/

# === OS ===
.DS_Store
Thumbs.db

# === IDE ===
.vscode/
.idea/
*.swp
*.swo
